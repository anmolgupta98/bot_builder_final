childname = <%= "childname" + (@child.id).to_s %>;
childnameform = <%= "childnameform" + (@child.id).to_s %>;
cross = <%= "cross" + (@child.id).to_s %>;
edit = <%= "editchildname" + (@child.id).to_s %>;
document.getElementById(childname.id).classList.add('hide');
document.getElementById(childnameform.id).classList.remove('hide');
document.getElementById(cross.id).classList.replace('col-8', 'col-1');
document.getElementById(edit.id).classList.add('hide');
