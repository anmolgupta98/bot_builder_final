define = <%= "child_define_id" + (@child.id).to_s %>;
expanded = <%= "child_expanded_id" + (@child.id).to_s %>;
<% connector_black_link_vertical = "connector_black_link_vertical" + (@child.parent_id).to_s%>;
document.getElementById(expanded.id).classList.remove('hide');
document.getElementById(define.id).classList.add('hide');
height = parseInt(document.getElementById('<%= connector_black_link_vertical %>').style.height);
height = height + 390;
document.getElementById('<%= connector_black_link_vertical %>').style.height = height + "px";
