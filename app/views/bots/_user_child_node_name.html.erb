<% if child %>
    <div class = "text-center">
        <div class = "Bot-Response ml-3 mt-2 mb-3" >
            <span><% if child.name %></span>
                <%= child.name %>
                <%= link_to image_tag('edit2.png', size: "17", class: "text-center", style: "margin-top: 0px;"), user_child_node_name_edit_icon_click_path(child.id), method: :get, remote: true  %>
            <% end %>
        </div>
    </div> 
<% end %>